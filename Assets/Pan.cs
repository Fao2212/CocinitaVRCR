using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pan : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    void WhenTriggerEnter(Collider other) { 
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
